{% macro nav_loop(page) %}
  {% import _self as macros %}
  {% for p in page.children.visible %}
    <li>
      <a href="{{ p.url }}" class="{{ active_page }}">
        {{ p.menu }}
      </a>
    </li>
  {% endfor %}
{% endmacro %}